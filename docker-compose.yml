version: '3.8'
services:
    nginx:
        container_name: nginx-vpp
        image: nginx:latest
        restart: always
        volumes:
            - ./html:/var/www/html
            - ./logs:/var/log/nginx/
            - ./sites-enabled:/etc/nginx/conf.d/
        depends_on:
            - php-fpm
            - mysql
    php-fpm:
        container_name: phpfpm-vpp
        image: php:7.4-fpm
        restart: always
        volumes:
            - ./html:/var/www/html
    mysql:
        container_name: mysql-vpp
        image: mysql:latest
        restart: always
        volumes:
            - ./mysql:/var/lib/mysql
        environment:
            MYSQL_ROOT_PASSWORD: root
